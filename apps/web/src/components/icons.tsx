/**
 * Central icon module. All icons come from lucide-react (tree-shakeable,
 * zero extra weight). Hover/tap animation is handled by the `.icon-anim`
 * CSS class (see index.css) — works on desktop (hover) and mobile (active).
 * Components import from here so swapping the source later is a one-file change.
 */
export {
  Play, Pause, Square, Settings, Search, Send, Plus, Folder, FolderOpen,
  FolderGit2, HelpCircle, Target, CheckSquare, Building2, RotateCcw,
  Loader2, X, ArrowLeft, ArrowRight, ChevronDown, ChevronRight,
  LayoutDashboard, Bot, KanbanSquare, CornerDownLeft, Check,
  Clock, XCircle, UserX, CheckCircle2, Brain, GitBranch, Workflow, Eye, List, Columns, Info,
  Pencil, Trash2, MessageCircle, Minus, History, ExternalLink, GitMerge, GitPullRequestClosed, AlertTriangle, FolderKanban,
  File, FileText, GitPullRequest, GitCommit, Upload, PanelRight, ScrollText, Code2, RefreshCw, Copy, Save, Code,
  Terminal, LogOut, Download, Home, Maximize2, Menu,
  Bell, MoreHorizontal, CheckCheck, Volume2,
} from 'lucide-react';
